_schema-version: "3.2"
ID: com-sap-mm-travelapp
description: A Fiori application.
version: 0.0.1
modules:
- name: com-sap-mm-travelapp-dest-content
  type: com.sap.application.content
  requires:
  - name: com-sap-mm-travelapp-dest-srv
    parameters:
      content-target: true
  - name: com-sap-mm-travelapp-repo-host
    parameters:
      service-key:
        name: com-sap-mm-travelapp-repo-host-key
  - name: com-sap-mm-travelapp-uaa
    parameters:
      service-key:
        name: com-sap-mm-travelapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: com-sap-mm-travelapp_repo_host
          ServiceInstanceName: com-sap-mm-travelapp-html5-srv
          ServiceKeyName: com-sap-mm-travelapp-repo-host-key
          sap.cloud.service: com-sap-mm-travelapp
        - Authentication: OAuth2UserTokenExchange
          Name: com-sap-mm-travelapp_uaa
          ServiceInstanceName: com-sap-mm-travelapp-xsuaa-srv
          ServiceKeyName: com-sap-mm-travelapp-uaa-key
          sap.cloud.service: com-sap-mm-travelapp
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: com-sap-mm-travelapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: com-sap-mm-travelapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comsapmmtravelapp.zip
      name: comsapmmtravelapp
      target-path: resources/
- name: comsapmmtravelapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: com-sap-mm-travelapp-dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: com-sap-mm-travelapp-dest-srv
    service-plan: lite
- name: com-sap-mm-travelapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: com-sap-mm-travelapp-xsuaa-srv
    service-plan: application
- name: com-sap-mm-travelapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: com-sap-mm-travelapp-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
